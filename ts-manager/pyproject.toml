[project]
name = "ts-manager"
version = "0.1.0"
description = "Manage Neovim treesitter parsers and queries without nvim-treesitter"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
ts-manager = "ts_manager.main:main"
